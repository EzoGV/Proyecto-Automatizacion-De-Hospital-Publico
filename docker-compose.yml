services:
  oracle-db:
    # Cambiamos la imagen por esta que es libre y automática
    image: gvenzl/oracle-free:latest
    container_name: oracle_hospital_db
    ports:
      - "1521:1521"
    environment:
      # Tu contraseña compleja
      - ORACLE_PASSWORD=Pax.,ytrG231
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data: