services:
  oracle-db:
    image: container-registry.oracle.com/database/free:latest
    container_name: oracle_hospital_db
    ports:
      - "1521:1521"
    environment:
      # Usamos tu contraseña deseada aquí
      - ORACLE_PASSWORD=Pax.,ytrG231
    volumes:
      - oracle_data:/opt/oracle/oradata

volumes:
  oracle_data: