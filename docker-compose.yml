services:
  oracle-db:
    image: gvenzl/oracle-free:latest
    container_name: oracle_hospital_db
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=Pax.,ytrG231  # 👈 Ponemos la misma clave de la nube
      - APP_USER=ADMIN               # 👈 Creamos el usuario ADMIN igual que en la nube
      - APP_USER_PASSWORD=Pax.,ytrG231
    shm_size: '1gb'